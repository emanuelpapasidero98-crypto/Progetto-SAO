.pragma library

const helpIcon = {
    normal: Qt.resolvedUrl("../Images/item/help.png"),
    hovered: Qt.resolvedUrl("../Images/item/help-hovered.png")
};

const fileIcon = {
    normal: Qt.resolvedUrl("../Images/item/file.png"),
    hovered: Qt.resolvedUrl("../Images/item/file-hovered.png")
};

const folderIcon = {
    normal: Qt.resolvedUrl("../Images/item/folder.png"),
    hovered: Qt.resolvedUrl("../Images/item/folder-hovered.png")
};
